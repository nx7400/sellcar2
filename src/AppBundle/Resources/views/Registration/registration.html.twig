{% extends "::base.html.twig" %}

{% block title %}Zarejestruj sie!{% endblock %}

{% block body %}

    <header>
        <div id="top_bar">
            <div id="logo">
                <button class="menu" onclick="openNav()">MENU</button>
            </div>
        </div>
    </header>

    <div id="mySidenav" class="sidenav">
        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
        <a href="{{ path('homepage') }}">Strona główna</a>
        <a href="{{ path('register') }}">Zarejestruj sie</a>
        <a href="{{ path('ad_index') }}">Zobacz ogłoszenia</a>
        <a href="{{ path('userPanel') }}">Panel uzytkownika</a>
    </div>

    <script>
        function openNav() {
            document.getElementById("mySidenav").style.width = "250px";
        }

        function closeNav() {
            document.getElementById("mySidenav").style.width = "0";
        }
    </script>

    <!--
        <div id="username_field">
            <label for="username">Użytkownik:</label>
            <input type="text" id="username" name="_username"/>
        </div>

        <div id="email_field">
            <label for="email">Email:</label>
            <input type="email" id="email" name="_email"/>
        </div>

        <div id="password_field">
            <label for="plainPassword.first">Hasło:</label>
            <input type="password" id="plainPassword.first" name="_plainPassword.first"/>
        </div>

        <div id="password_field">
            <label for="plainPassword.second">Potwierdź hasło:</label>
            <input type="password" id="plainPassword.second" name="_plainPassword.second"/>
        </div>
    !-->

    <div id="form">
    <h1>Zarejestruj się!</h1>
    {{ form_start(form) }}

    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword.first) }}
    {{ form_row(form.plainPassword.second) }}

    <button type="submit">Register!</button>
    </div>
    {{ form_end(form) }}

{% endblock %}

{% block stylesheets %}
    <style>
        body { background: #F5F5F5; font: 18px/1.5 sans-serif; }
        h1, h2 { line-height: 1.2; margin: 0 0 .5em; }
        h1 { background:#F5F5F5; font-size: 36px; }
        h2 { font-size: 21px; margin-bottom: 1em; }
        p { margin: 0 0 1em 0; }
        a { color: #0000F0; }
        a:hover { text-decoration: none; }
        code { background: #F5F5F5; max-width: 100px; padding: 2px 6px; word-wrap: break-word; }

        #form{
            text-align: center;
        }

        .menu {
            position: relative;
            background-color: #99CDD8;
            border: none;
            font-size: 28px;
            color: #000000;
            padding: 20px;
            width: 200px;
            text-align: center;
            text-decoration: none;
            overflow: hidden;
            cursor: pointer;
        }

        #top_bar{
            height: 80px;
            background-color: #2C3E50;
            margin-bottom: 5px;
            box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);

        }

        #logo{
            height: 80px;
            width: 200px;
            background-color: #99CDD8;
            text-align: center;
        }

        #logo:hover{
            box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2)
        }

        .sidenav {
            height: 100%;
            width: 0;
            position: fixed;
            z-index: 1;
            top: 0;
            left: 0;
            background-color: #111;
            overflow-x: hidden;
            padding-top: 60px;
            transition: 0.5s;
        }

        .sidenav a {
            padding: 8px 8px 8px 32px;
            text-decoration: none;
            font-size: 25px;
            color: #818181;
            display: block;
            transition: 0.3s
        }

        .sidenav a:hover, .offcanvas a:focus{
            color: #f1f1f1;
        }

        .sidenav .closebtn {
            position: absolute;
            top: 0;
            right: 25px;
            font-size: 36px;
            margin-left: 50px;
        }


        @media screen and (max-height: 450px) {
            .sidenav {padding-top: 15px;}
            .sidenav a {font-size: 18px;}
        }


    </style>
{% endblock %}
